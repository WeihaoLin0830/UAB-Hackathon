---
// ChatPanel component - handles the chat interface
---

<section class="chat-panel">
    <div class="chat-messages">
        <div class="message bot-message">
            Hola, soy tu asistente virtual. ¿En qué puedo ayudarte hoy?
        </div>
        <div class="message user-message">
            ¿Podrías explicarme cómo funciona este playground?
        </div>
        <div class="message bot-message">
            Por supuesto. Este playground te permite interactuar con un asistente virtual no funcional
        </div>
    </div>
    <div class="chat-input">
        <input type="text" placeholder="Escribe tu mensaje aquí..." class="text-input">
        <button class="send-button">
            <svg width="28" height="28" viewBox="-2.4 -2.4 28.80 28.80" fill="none" xmlns="http://www.w3.org/2000/svg" class="send-icon">
                <path d="M11.5003 12H5.41872M5.24634 12.7972L4.24158 15.7986C3.69128 17.4424 3.41613 18.2643 3.61359 18.7704C3.78506 19.21 4.15335 19.5432 4.6078 19.6701C5.13111 19.8161 5.92151 19.4604 7.50231 18.7491L17.6367 14.1886C19.1797 13.4942 19.9512 13.1471 20.1896 12.6648C20.3968 12.2458 20.3968 11.7541 20.1896 11.3351C19.9512 10.8529 19.1797 10.5057 17.6367 9.81135L7.48483 5.24303C5.90879 4.53382 5.12078 4.17921 4.59799 4.32468C4.14397 4.45101 3.77572 4.78336 3.60365 5.22209C3.40551 5.72728 3.67772 6.54741 4.22215 8.18767L5.24829 11.2793C5.34179 11.561 5.38855 11.7019 5.407 11.8459C5.42338 11.9738 5.42321 12.1032 5.40651 12.231C5.38768 12.375 5.34057 12.5157 5.24634 12.7972Z" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"></path>
            </svg>
        </button>
    </div>
</section>

<style>
    .text-input {
        stroke: var(--text-color);
    }

    h1 {
        font-size: 20px;
        font-weight: 700;
        color: var(--text-color);
        font-family: 'Poppins', sans-serif;
        font-stretch: expanded;
        letter-spacing: 0.2px;
    }
    
    .chat-panel {
        position: fixed;
        display: flex;
        flex-direction: column;
        background-color: var(--panel-color);
        height: calc(100vh - 60px);
        width: 60vh;
        overflow: hidden;
        border-right: 1px solid var(--border-color);
        box-shadow: var(--shadow-md);
    }

    .chat-actions {
        display: flex;
        gap: 8px;
    }

    .action-button {
        background: transparent;
        border: 1px solid #ddd;
        color: var(--text-color);
        padding: 4px;
        border-radius: var(--radius-sm);
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s;
    }

    .action-button:hover {
        border-color: var(--accent-color);
        color: var(--accent-color);
        scale: 1.1;
    }

    .chat-messages {
        flex: 1;
        padding: 24px 14px 24px 24px;
        padding-bottom: 80px;
        overflow-y: auto;
        background-color: var(--bg-color);
        display: flex;
        flex-direction: column;
        position: relative;
    }

    .message {
        margin-bottom: 22px;
        max-width: 85%;
        padding: 14px 18px;
        line-height: 1.6;
        font-size: 15px;
        transition: transform 0.2s;
    }

    .message:hover {
        transform: translateY(-2px);
    }

    .user-message {
        background-color: var(--panel-color);
        border: 1px solid var(--border-color);
        margin-left: auto;
        border-radius: 14px;
        border-bottom-right-radius: 4px;
        box-shadow: var(--shadow-sm);
    }

    .bot-message {
        background-color: transparent;
        margin-right: auto;
        color: var(--text-color);
        padding-left: 0;
        padding-right: 0;  /* Remove horizontal padding */
        padding-top: 0;    /* Remove top padding */
        padding-bottom: 0; /* Remove bottom padding */
        border: none;
        max-width: 95%;    /* Allow bot messages to be wider */
    }
    
    .bot-message:hover {
        transform: none;   /* Remove hover effect for bot messages */
    }
    
    .chat-input {
        padding: 20px 20px;
        margin-top: auto;
        background-color: var(--bg-color);
        display: flex;
        align-items: center;
        z-index: 10;
        position: relative;
    }

    .chat-input .text-input {
        flex: 1;
        padding: 16px 20px;
        color: var(--text-color);
        border: 2px solid var(--border-color);
        border-radius: 50px;
        font-size: 15px;
        background-color: white;
        transition: all 0.3s;
        padding-right: 50px;
        box-shadow: var(--shadow-md);
        outline: none;
    }

    .chat-input .text-input:focus {
      
        border-color: var(--accent-color);
        box-shadow: 0 0 0 3px var(--accent-light);
    }

    .chat-input .text-input::placeholder {
        color: var(--placeholder-color);
    }

    .send-button {
        width: 44px;
        height: 44px;
        background-color: transparent;
        color: white;
        border: none;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s;
        position: absolute;
        right: 25px;
        cursor: pointer;
    }
    
    .send-icon {
        color: var(--accent-color);
    }

    .send-icon:hover {
        transform: scale(1.1);
        transition: all 0.3s;
    }

    /* Add this if you want the icon to be green instead of white */
    /*
    .send-button {
        background-color: transparent;
    }
    
    .send-icon {
        color: green;
    }
    */

    @media (max-width: 900px) {
        .chat-panel {
            height: 60vh;
            border-radius: var(--radius) var(--radius) 0 0;
        }
        
        .chat-input {
            padding: 16px 16px;
        }
    }
</style>


